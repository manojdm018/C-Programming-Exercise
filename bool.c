#include <stdio.h>
#include <stdbool.h>

int main (void) {
    bool isAlive = true;
    bool isDead = false;

    if (isAlive)
    {
        printf("Alaxander graham bell is dead"); 
    }
    else
    {
        printf("HE is not dead");
    }
}